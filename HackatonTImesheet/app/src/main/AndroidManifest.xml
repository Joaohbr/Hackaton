<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="br.com.zup.hackatontimesheet">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="@string/app_name"
        android:name=".application.TimesheetApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".login.ui.LoginActivity"
            android:label="@string/app_name"
            android:screenOrientation="portrait"
            android:theme="@style/LoginTheme">
            <intent-filter>
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.DEFAULT" />
                <action android:name="android.intent.action.MAIN" />
            </intent-filter>
        </activity>

        <activity
            android:name=".home.ui.HomeActivity"
            android:screenOrientation="portrait"
            android:label="@string/app_name">
        </activity>

        <activity android:name=".refund_report.ui.RefundReportActivity"
            android:screenOrientation="portrait"
            android:label="@string/title_refund_report_activity">
        </activity>

        <activity android:name=".refund_entry.ui.RefundEntryActivity"
            android:screenOrientation="portrait"
            android:label="@string/title_refund_entry_activity">
        </activity>
    </application>

</manifest>